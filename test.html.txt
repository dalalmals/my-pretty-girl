<!doctype html>
<html lang="en">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width,initial-scale=1">
<title>my pretty girl 💖</title>
<style>
  body {
    margin: 0;
    font-family: 'Segoe UI', Tahoma, sans-serif;
    background: linear-gradient(135deg, #ffdde1 0%, #ee9ca7 100%);
    color: #333;
    display: flex;
    align-items: center;
    justify-content: center;
    min-height: 100vh;
    padding: 20px;
  }
  .container {
    background: rgba(255,255,255,0.9);
    backdrop-filter: blur(6px);
    border-radius: 16px;
    padding: 30px 40px;
    max-width: 700px;
    box-shadow: 0 10px 40px rgba(0,0,0,0.15);
    animation: fadeIn 1.2s ease both;
  }
  h1 {
    text-align: center;
    font-size: 2.2rem;
    margin-bottom: 20px;
    color: #d6336c;
  }
  p {
    line-height: 1.6;
    font-size: 1.05rem;
    margin-bottom: 18px;
  }
  .question {
    margin-top: 25px;
    text-align: center;
    font-size: 1.6rem;
    font-weight: bold;
    color: #6a1b9a;
    opacity: 0;
    transform: translateY(20px);
    animation: slideUp 1s ease forwards;
    animation-delay: 3s;
  }
  .buttons {
    margin-top: 20px;
    display: flex;
    justify-content: center;
    gap: 15px;
    opacity: 0;
    transform: translateY(20px);
    animation: slideUp 1s ease forwards;
    animation-delay: 3.5s;
  }
  button {
    padding: 12px 24px;
    border: none;
    border-radius: 8px;
    cursor: pointer;
    font-size: 1rem;
    font-weight: bold;
    transition: transform 0.15s ease;
  }
  button:active {
    transform: scale(0.97);
  }
  .yes {
    background: #ff4d88;
    color: white;
  }
  .no {
    background: #ddd;
    color: #333;
  }
  .result {
    margin-top: 25px;
    text-align: center;
    font-size: 1.4rem;
    color: #28a745;
    display: none;
  }
  canvas {
    position: fixed;
    top: 0; left: 0;
    pointer-events: none;
    width: 100%; height: 100%;
  }
  @keyframes fadeIn {
    from {opacity:0; transform: scale(0.98);}
    to {opacity:1; transform: scale(1);}
  }
  @keyframes slideUp {
    to {opacity: 1; transform: translateY(0);}
  }
</style>
</head>
<body>
<canvas id="confetti"></canvas>

<div class="container">
  <h1>my pretty girl 💖</h1>
  <p>hey stinky, i have something really important to ask you, you’ll find out in a sec i just need to tell you all of this first, i really realy like you and oops you so much, ever since we started talking again i haven’t stopped thinking about you and how special you are to me, time with you flies so fast and i forget about all my problems when im with you, you make me feel safe and loved so much, you make regular things seem brighter (cuz ur my sun), i literally can’t get enough of you even when we spend a whole day together, you’re everything to me and i want to take this moment to ask you..</p>

  <div class="question">Will you be my girlfriend? 💍</div>

  <div class="buttons">
    <button class="yes" id="yesBtn">Yes 💖</button>
    <button class="no" id="noBtn">No 😢</button>
  </div>

  <div class="result" id="result">She said YES! 🎉💖</div>
</div>

<script>
const yesBtn = document.getElementById('yesBtn');
const noBtn = document.getElementById('noBtn');
const result = document.getElementById('result');

yesBtn.addEventListener('click', () => {
  result.textContent = "She said YES! 🎉💖";
  result.style.display = 'block';
  startConfetti();
});
noBtn.addEventListener('click', () => {
  result.textContent = "She said no 😢 but you’re still amazing 💪";
  result.style.display = 'block';
});

function startConfetti(){
  const canvas = document.getElementById('confetti');
  const ctx = canvas.getContext('2d');
  canvas.width = window.innerWidth;
  canvas.height = window.innerHeight;
  let pieces = [];
  for (let i = 0; i < 100; i++) {
    pieces.push({
      x: Math.random() * canvas.width,
      y: Math.random() * canvas.height - canvas.height,
      w: 10,
      h: 10,
      color: hsl(${Math.random()*360}, 100%, 50%),
      speed: Math.random() * 3 + 2
    });
  }
  function update(){
    ctx.clearRect(0,0,canvas.width,canvas.height);
    pieces.forEach(p => {
      p.y += p.speed;
      if (p.y > canvas.height) p.y = -10;
      ctx.fillStyle = p.color;
      ctx.fillRect(p.x, p.y, p.w, p.h);
    });
    requestAnimationFrame(update);
  }
  update();
}
</script>
</body>
</html>